// for...in loop in objects
const persion={
    name:"venki",
    age:19,
    fielf:"pro&edit"

};

for(let keys in persion){
    // console.log(keys,persion[keys]);
}

// for..in in Arrays
let arr=[1,2,3,4,5,6];
for(let i in arr){
    // console.log(i,arr[i]);
}

// task

let obj={a:1,n:2,c:3};
for(let k in obj){
    console.log(k,obj[k]);
}